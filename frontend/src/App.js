import useMongoDB from './hooks/useMongoDB';

function App() {
  // const { findDocuments } = useMongoDB();

  return (
    <div className="App">
      <p>hello world</p>
      {/* <button onClick={() => findDocuments()}>find documents</button> */}
    </div>
  );
}

export default App;
